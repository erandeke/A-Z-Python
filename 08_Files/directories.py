import os

print(os.getcwd());

print(os.listdir())  # directories.py', '__init__.py', 'Readme.md', 'test.txt', 'FileOperations.py'

# Make directory
# os.mkdir("folder"); #This should create folder directory

# Renaming directory
#os.rename('folder', 'new_folder');

# Remove the directory

os.mkdir("test");